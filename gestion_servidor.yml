---
- name: Gestion Integral del Servidor
  hosts: all
  become: yes
  become_method: sudo
  become_user: root

  vars:
    # Contenedores críticos para auditoría
    contenedores_criticos:
      - mariadb
      - netdata
      - nextcloud
      - postgres
      - wp-lnt
      - wp-ygb
  tasks:
    # TAREA DE DIAGNÓSTICO
    - name: "DEBUG: Valor de la variable accion"
      debug:
        msg: "La accion recibida es: {{ accion | default('VACÍO') }}"

    # 1. ACCIÓN: INICIAR
    - name: Iniciar contenedores específicos
      docker_container:
        name: "{{ item | trim }}"
        state: started
      loop: "{{ (contenedor_nombre | default('')).split(',') }}"
      when: 
        - (accion | default('') is search('levantar', ignorecase=True) or accion | default('') is search('Iniciar', ignorecase=True))
        - contenedor_nombre | default('') | trim | length > 0

    # 2. ACCIÓN: REINICIAR
    - name: Reiniciar contenedores específicos
      docker_container:
        name: "{{ item | trim }}"
        state: started
        restart: yes
      loop: "{{ (contenedor_nombre | default('')).split(',') }}"
      when: 
        - (accion | default('') is search('reiniciar', ignorecase=True))
        - contenedor_nombre | default('') | trim | length > 0

    # 3. ACCIÓN: VER LOGS
    - name: Obtener logs de contenedores
      shell: "docker logs --tail 100 {{ item | trim }}"
      register: docker_logs
      loop: "{{ (contenedor_nombre | default('')).split(',') }}"
      when: 
        - (accion | default('') is search('ver_logs', ignorecase=True) or accion | default('') is search('Logs', ignorecase=True))
        - contenedor_nombre | default('') | trim | length > 0
      failed_when: false

    - name: Mostrar logs en Semaphore
      debug:
        msg: "{{ item.stdout_lines }}"
      loop: "{{ docker_logs.results | default([]) }}"
      when: 
        - (accion | default('') is search('ver_logs', ignorecase=True) or accion | default('') is search('Logs', ignorecase=True))
        - item.stdout_lines is defined

    # 4. ACCIÓN: LIBERAR SWAP
    - name: Vaciar y reactivar memoria SWAP
      shell: "swapoff -a && swapon -a"
      when: (accion | default('') is search('liberar_swap', ignorecase=True) or accion | default('') is search('Liberar', ignorecase=True))

    # 5. ACCIÓN: LISTAR
    - name: Obtener info de Docker
      docker_host_info:
        containers: yes
        containers_all: yes
      register: docker_info
      when: (accion | default('') is search('listar', ignorecase=True) or accion | default('') is search('Listar', ignorecase=True))

    - name: Mostrar lista de todos los contenedores
      debug:
        msg: "-> {{ item.Names[0] | replace('/', '') }} - {{ item.Status }}"
      loop: "{{ docker_info.containers | default([]) }}"
      when: 
        - (accion | default('') is search('listar', ignorecase=True) or accion | default('') is search('Listar', ignorecase=True))
        - docker_info.containers is defined
      loop_control:
        label: "{{ item.Names[0] | replace('/', '') }}"

    # 6. ACCIÓN: AUDITAR (Correo y Telegram)
    - name: Obtener info para auditoria
      docker_host_info:
        containers: yes
        containers_all: yes
      register: docker_audit
      when: (accion | default('') is search('auditar', ignorecase=True))

    - name: Filtrar criticos caidos y limpiar formato
      set_fact:
        lista_caidos_texto: >-
          {% set caidos = [] %}
          {% for c in (docker_audit.containers | default([])) %}
            {% set nombre = c.Names[0] | replace('/', '') %}
            {% if nombre in contenedores_criticos and 'Up' not in c.Status %}
              {% set _ = caidos.append(nombre) %}
            {% endif %}
          {% endfor %}
          {{ caidos | join(', ') }}
      when: (accion | default('') is search('auditar', ignorecase=True))

    - name: Enviar correo de alerta
      community.general.mail:
        host: mail.proyectolanuevatierra.com
        port: 587
        secure: starttls
        username: admin@proyectolanuevatierra.com
        from: admin@proyectolanuevatierra.com
        password: e9gcb3pgedzo3lovwgd8jjn4wezryw2c4e2pdbjxpsfo02neuggdiv67g13qogv1
        to: lauragonzalez.150463@gmail.com
        subject: "⚠️ ALERTA: Contenedores caídos en {{ inventory_hostname }}"
        body: |
          Se han detectado servicios críticos fuera de servicio.
          Contenedores afectados: {{ lista_caidos_texto }}
          Por favor, revisa Semaphore.
      when: 
        - (accion | default('') is search('auditar', ignorecase=True))
        - lista_caidos_texto | trim != ""

    # 7. ACCIÓN: ACTUALIZAR
    - name: Actualizar sistema
      apt:
        update_cache: yes
        upgrade: dist
      when: (accion | default('') is search('actualizar', ignorecase=True) or accion | default('') is search('Actualizar', ignorecase=True))

    # 8. ACCIÓN: BACKUP DE BASE DE DATOS
    - name: Crear carpeta de backups
      file:
        path: "/home/soporte_ansible/backups_db"
        state: directory
        owner: soporte_ansible
        group: soporte_ansible
        mode: '0755'
      when: (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))

    - name: Ejecutar volcado de MariaDB
      shell: |
        set -o pipefail
        docker exec mariadb mariadb-dump -u root --password='Torpedin01@' --all-databases | gzip > /home/soporte_ansible/backups_db/db_backup_{{ ansible_date_time.date }}_{{ ansible_date_time.hour }}{{ ansible_date_time.minute }}.sql.gz
      args:
        executable: /bin/bash
      when: (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))

    - name: Listar copias existentes
      shell: "ls -lh /home/soporte_ansible/backups_db/"
      register: ls_res
      when: (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))

    - name: Mostrar archivos en Semaphore
      debug:
        msg: "{{ ls_res.stdout_lines }}"
      when: (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))

    - name: Borrar copias de más de 7 días
      find:
        paths: "/home/soporte_ansible/backups_db"
        age: 7d
        patterns: "*.sql.gz"
      register: archivos_viejos
      when: (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))

    - name: Eliminar archivos caducados
      file:
        path: "{{ item.path }}"
        state: absent
      loop: "{{ archivos_viejos.files | default([]) }}"
      when: 
        - (accion | default('') is search('backup', ignorecase=True) or accion | default('') is search('copia', ignorecase=True))
        - archivos_viejos.matched > 0
